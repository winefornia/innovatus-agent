# Core
langgraph>=0.2.0           # INVOICE pipeline only (tasting room is on Vertex ADK)
langchain-core>=0.2.0
langchain-anthropic>=0.2.0
anthropic>=0.30.0

# Tasting-room coordinator — Google Vertex ADK agent (Claude via LiteLLM)
google-adk>=1.0.0
litellm>=1.83.0            # avoid 1.82.7-1.82.8 (March 2026 security advisory)

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0
python-multipart>=0.0.9

# Square
squareup>=37.0.0

# Config
python-dotenv>=1.0.0
tzdata>=2024.1            # IANA tz data for zoneinfo (CI / slim images lack system tzdata)

# Database
supabase>=2.4.0
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary,pool]>=3.1.0

# Gmail / Google OAuth
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# Memory (Mem0 persistent user memory)
mem0ai>=1.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# Observability (optional)
# langsmith>=0.1.0
